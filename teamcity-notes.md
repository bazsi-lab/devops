HTL - Teamcity Server and Agent


## Agents - Autostart

Agents stop to work if the VM was switched off, but they never start automatically again. To start the agents automatically at systemstart, we set up them as systemd services.

